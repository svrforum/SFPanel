# eslint 10 was released ahead of typescript-eslint 9; the latter still
# declares its eslint peer as ^9. The runtime is compatible, but npm@10's
# default install fails the peer check. Once typescript-eslint 9 ships,
# remove this file.
legacy-peer-deps=true
